#include <stdio.h>
#define p printf
#define s scanf
//primero se valida la condicion

int main(){
    int num;
    while(num != -1){
        p("Ingrese un n%cmero: ",163);
        s("%d",&num);
    }
    return 0;
}
